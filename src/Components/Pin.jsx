

const Pin = ({pinSize}) => {
    return (
        <div className={`pin ${pinSize}`}>

            <div className="content">
                <h1>hello</h1>

            </div>
            
        </div>
    );
};

export default Pin;